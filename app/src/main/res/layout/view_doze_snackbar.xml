<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/doze_snackbar_card"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="@dimen/item_size_medium"
    android:layout_marginStart="@dimen/padding_large"
    android:layout_marginEnd="@dimen/padding_large"
    android:layout_marginTop="@dimen/item_size_large"
    android:layout_centerHorizontal="true"
    app:cardCornerRadius="@dimen/default_rounded_corner"
    app:cardBackgroundColor="@color/cardview_dark_background"
    app:cardElevation="10dp"
    android:clipToPadding="false">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="24dp"
        android:paddingEnd="12dp">
        <cache.doze.Views.PowerTextView
            android:id="@+id/text_description"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="@dimen/padding_medium"
            android:layout_marginBottom="@dimen/padding_medium"
            android:layout_toStartOf="@id/button_close_wrapper"
            android:text="Alert"
            android:textSize="16sp"
            android:textColor="@color/light_cream"
            android:maxLines="2"
            android:lines="2"
            android:ellipsize="end"
            android:gravity="center_vertical"/>

        <RelativeLayout
            android:id="@+id/button_close_wrapper"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?actionBarItemBackground">
            <!--<View-->
                <!--android:layout_width="24dp"-->
                <!--android:layout_height="24dp"-->
                <!--android:layout_centerInParent="true"-->
                <!--/>-->

            <ImageView
                android:id="@+id/button_close"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:background="@drawable/baseline_close_black_24"
                android:backgroundTint="@color/light_cream"
                android:layout_centerInParent="true"
                android:layout_marginStart="24dp"/>
        </RelativeLayout>


    </RelativeLayout>


</android.support.v7.widget.CardView>